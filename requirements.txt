# DSB Group 8 — Python dependencies
# Reproduce with:  python -m pip install -r requirements.txt
# Then register a kernel:  python -m ipykernel install --user --name python3
# Run a notebook headless:  jupyter nbconvert --to notebook --execute --inplace <notebook>.ipynb

# data + analysis
pandas>=2.0
numpy>=1.24
scipy>=1.10

# plotting
matplotlib>=3.7
seaborn>=0.12

# Excel workbook
openpyxl>=3.1

# notebook tooling (run / convert)
jupyter>=1.0
notebook>=7.0
nbconvert>=7.0
nbformat>=5.7
ipykernel>=6.0
